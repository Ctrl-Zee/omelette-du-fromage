import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'omelette-du-fromage',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
